# Decision Log

실제로 대안이 있었던 결정만 남긴다. 결론은 `README.md`에, 여기에는 **왜 다른 길을 버렸는지**를 적는다.

---

## [2026-09-04] tool_runner 실행 루프 → 의도 추론 1회 + 결정적 실행기

```
Decision:            모델이 툴을 직접 실행하는 SDK tool_runner 루프를 걷어내고, 모델은 Intent JSON
                     하나만 반환하게 했다. 게이트·실행·응답 문장은 로컬 결정적 코드가 맡는다.
Reason:              온디바이스 엣지 AI 가정에서 왕복 횟수가 그대로 비용이자 지연이다. tool_runner는
                     툴 호출 수만큼 왕복하지만 새 구조는 1회로 고정된다. 안전 판정과 응답 문장은
                     모델의 판단이 필요한 일이 아니라 규칙으로 표현되는 일이다.
Alternatives:        (a) tool_runner 유지 (b) tool_runner 유지 + 확인 게이트만 강화
Rejected because:    (a) 왕복이 모델 판단에 따라 늘어나 온디바이스 지연 예산을 세울 수 없다.
                     (b) 게이트를 고쳐도 응답 문장 생성에 왕복이 하나 더 붙는 문제가 남는다.
Risks:               모델이 대화 흐름을 직접 관리하지 않으므로 멀티턴 상태(pending)를 우리가 들고
                     있어야 한다. 상태 관리 버그는 이제 우리 책임이다.
Future improvement:  parse_intent()가 LLM을 쓰는 유일한 지점이다. 실제 온디바이스 경량 모델로
                     교체할 때 이 함수만 바꾸면 되는지 실측으로 확인할 것.
```

## [2026-09-04] 값이 없는 발화는 실행하지 않고 되묻는다

```
Decision:            "조명 좀 어둡게", "의자 좀 세워줘"처럼 방향은 있으나 숫자가 없는 발화는
                     기본 스텝으로 실행하지 않고 되묻는다. "에어컨 온도 좀 높여줘"도 마찬가지라
                     되묻기 1턴이 붙는다.
Reason:              이 프로토타입이 다루는 문제 자체가 "모르겠을 때 무엇을 하는가"다. 기본 스텝을
                     넣으면 미호출 정확도 지표에 남는 건 인젝션·미지원기능 케이스뿐이라 지표가
                     측정하려던 것을 더 이상 측정하지 못한다.
Alternatives:        (a) 방향이 있으면 기본 스텝(온도 ±3, 밝기 ±20)으로 실행
                     (b) 전부 기본 스텝 실행 — "좀 춥다"도 온도 +3
Rejected because:    (a)(b) 모두 UX는 자연스러워지지만 차량에서 틀린 실행의 비용이 되묻기 1턴의
                     비용보다 크다는 전제와 어긋난다. (b)는 상태 서술과 명령을 구분하지 않는다.
Risks:               데모에서 되묻기가 답답해 보일 수 있다. 실사용이라면 "직전 스텝 기억" 같은
                     보완이 필요하다.
Future improvement:  기본 스텝을 넣더라도 미호출 정확도를 별도 지표로 유지할 것. 지표를 지우면서
                     UX를 얻는 교환은 하지 말 것.
```

## [2026-09-04] 확인 대기(pending)를 로컬에만 보관

```
Decision:            확인이 필요한 동작의 실행 인자를 로컬 pending에만 두고, 모델은 confirm:yes까지만
                     말할 수 있게 했다. 실행은 pending["args"]로만 한다.
Reason:              초판은 confirmed를 모델이 채우는 툴 인자로 뒀는데, 모델이 첫 호출부터
                     confirmed=True를 넣으면 게이트가 그대로 열렸다. "툴이 거부하면 못 어긴다"는
                     주장이 반만 성립하던 상태였다.
Alternatives:        (a) 초판 유지 — 프롬프트로 "동의 전에는 confirmed=True 금지"를 지시
                     (b) nonce 토큰 발급 후 그 토큰을 든 호출만 실행
Rejected because:    (a) 프롬프트 지시는 모델이 어길 수 있고, 어겼는지 코드가 알 수 없다.
                     (b) 동작은 하지만 토큰 수명·저장소를 따로 관리해야 해서 되묻기 상태와 두 벌이
                     된다. 되묻기와 확인을 같은 pending으로 합치면 상태 관리 코드가 한 벌이다.
Risks:               pending이 세션 메모리에만 있어 프로세스가 죽으면 확인 대기가 사라진다.
                     실차라면 IVI 재시작 시 대기 상태를 어떻게 할지 정해야 한다.
Future improvement:  pending에 만료 시간이 없다. 확인 질문 후 한참 뒤의 "응"도 실행으로 처리된다.
```

## [2026-09-04] 상대값은 모델이 아니라 실행기가 해석

```
Decision:            모델은 relative=true, value=3까지만 내고, 현재 24도에 더해 27도를 만드는 계산과
                     범위 클램프는 _resolve()가 한다.
Reason:              모델에 현재 상태를 넘기지 않아도 되고, 산술이 어긋날 수 없고, 답변에
                     "(24 → 27)"을 정확히 붙일 수 있다.
Alternatives:        모델에 현재 온도를 주고 절대값을 직접 계산하게 한다.
Rejected because:    프롬프트에 변하는 상태가 더 들어가 캐시가 깨지고, 검증할 수 없는 산술을 모델에
                     맡기게 된다. 잘못된 절대값은 스키마 검증을 통과한다.
Risks:               모델이 relative 플래그를 잘못 채우면 증감이 절대값으로 해석된다("3도 올려"가
                     3도 설정이 됨). 아직 실측으로 확인하지 못한 실패 모드다.
Future improvement:  --eval에서 relative 오분류를 별도로 세어볼 것. 상대값 케이스가 현재 2건뿐이다.
```

## [2026-09-04] 로컬 ASR을 선택적 배선이 아니라 실제 의존성으로 채택

```
Decision:            faster-whisper를 requirements.txt에 넣고 --listen 경로를 실제로 동작시킨다.
Reason:              "음성을 로컬에서 처리한다"가 이 프로젝트의 주장 중 하나인데, 인터페이스만
                     두고 실행하지 않으면 코드로 뒷받침되지 않는 주장이 된다.
Alternatives:        import를 선택적으로 두고 미설치 시 안내만 — 핵심 평가는 텍스트 경로로 유지.
Rejected because:    재현 비용은 낮아지지만 검증되지 않은 주장이 하나 남는다.
Risks:               클린 체크아웃에서 첫 --listen 실행 시 모델을 내려받는다(base ≈ 145MB).
                     --selftest와 --eval은 ASR을 타지 않으므로 핵심 재현 경로는 영향 없다.
Future improvement:  주행 소음이 섞인 실제 녹음으로 전사 정확도를 재볼 것. 현재 표본은 TTS 1건이다.
```

## [2026-09-04] Intent enum을 영어 식별자로

```
Decision:            domain/action 값을 climate·set_temperature 같은 영어 식별자로 둔다.
Reason:              기존 기능 이름(set_climate_temperature 등)과 1:1로 붙어 dispatch 코드에 매핑
                     테이블이 한 겹 덜 생긴다.
Alternatives:        "공조"·"온도조절" 같은 한국어 값 (원래 스펙 예시가 이 형태였다).
Rejected because:    JSON만 봐도 읽히는 장점은 있으나, 한국어→기능이름 매핑 테이블이 추가로 필요하고
                     그 테이블이 곧 오타 지점이 된다.
Risks:               JSON 출력만 보면 한국어 사용자에게 덜 직관적이다. 답변 문장은 한국어라 실사용엔
                     영향 없다.
```

## [2026-09-07] 목적지 변경 확인 게이트 제거

```
Decision:            "주행 중 + 기존 목적지 있음"일 때 목적지 변경에 확인을 요구하던 게이트를
                     없앴다. 목적지 설정은 이제 항상 즉시 실행된다.
Reason:              이 차는 자율주행이 아니라 사람이 계속 운전대를 잡고 있다는 전제다. 목적지가
                     바뀐다고 차가 알아서 위험하게 움직이지 않고, "다시 원래 목적지로" 한마디면
                     되돌릴 수 있다. 통화·메시지(차 밖으로 나가면 되돌릴 수 없음)나 등받이(벨트
                     유효성 저하라는 물리적 문제)와 위험 범주 자체가 다르고, README에도 왜
                     위험한지 근거가 안 적혀 있었다 — 안전 근거 없이 확인 한 턴만 추가해 오히려
                     발화 1건으로 끝날 상호작용을 2턴으로 늘리고 있었다. 실제 순정 내비·Waze·
                     Google Maps도 주행 중 음성 목적지 변경에 별도 확인을 안 건다.
Alternatives:        (a) 게이트 유지 (b) "실제로 다른 목적지로 바뀔 때만" 조건을 좁혀서 유지
Rejected because:    (a) 안전 근거가 없는데 확인 턴만 추가해 주행 중 음성 상호작용을 늘린다 —
                     목표(주행 중 상호작용 최소화)에 역행한다.
                     (b)도 "왜 확인이 필요한가"에 답을 못한다. 조건을 좁혀도 근거 없는 게이트는
                     근거 없는 게이트다.
Risks:               오인식으로 엉뚱한 목적지가 바로 반영될 수 있다. 다만 통화·메시지처럼 차
                     밖으로 나가거나 되돌릴 수 없는 동작이 아니라, "다시 원래 목적지로" 한마디로
                     되돌릴 수 있는 비용이 낮은 실수다.
Future improvement:  경로 이탈이 임박한 순간(예: 다음 교차로 직전)에 목적지가 바뀌면 안내가
                     급격히 바뀔 수 있다 — 이건 확인이 아니라 "다음 구간부터 반영" 같은 지연
                     적용으로 다룰 문제다. 이번 프로토타입 범위 밖.
```

---

# Research Memo

## [2026-09-04] 로컬 ASR 지연 측정

### Summary
faster-whisper base 모델이 인캐빈 응답 예산 안에 들어오는지 확인.

### Findings
CPU int8, 2.4초 한국어 발화(Windows TTS 생성) 기준 전사 정확. 첫 실행은 모델 다운로드 포함 8.2초.

### What improved
워밍업 후 전사 지연 **0.67초 중앙값** (n=3). 모델을 프로세스 수명 동안 재사용하므로 실사용 경로는
워밍업 후 수치가 맞다.

### What regressed
없음.

### Remaining failures
표본이 TTS 1건이라 정확도를 주장할 수 없다. 주행 소음·실제 화자 조건 미검증.

### Next experiment
tiny 모델과 비교해 정확도 손실 대비 지연 이득을 볼 것. 단, 의도 추론 지연이 아직 미측정이라
전체 예산에서 ASR이 차지하는 비중을 모른다 — `--eval` 먼저.

---

# 현재 상태 (2026-09-07 기준)

- 구조 재설계 완료, `--selftest` 통과, 공개 저장소 푸시 완료.
- 게이트 채점(`gate_accuracy`) 추가 완료, 목적지 변경 확인 게이트 제거(위 결정 참고).
- SYSTEM 프롬프트에 프롬프트 캐싱(`cache_control`) 적용 완료 — 실제 캐시 적중 여부는 아직 확인 안 함.
- **`--eval` 실측 완료.** tune(n=14)·holdout(n=9) 모두 tool/args/gate/abstain **100%**.
  지연 p50 2.68~2.70s, p95 3.09~6.41s (effort=low, Claude Opus 5). README "측정된 것" 표에 반영.
- API 비용 지출 **$0.07** (23건 tune+holdout, Claude Opus 5, effort=low). 사용한 키는 테스트 직후 폐기함.

## 다음 세션에서 할 일 (우선순위 순)

1. **캐시 적중 확인** — `response.usage.cache_read_input_tokens`로 SYSTEM 캐싱이 실제로 걸렸는지 확인.
   Opus 5 최소 캐시 길이는 512토큰인데 SYSTEM은 560자라 애매한 경계 — 실측 전엔 모른다.
2. relative 오분류 별도 집계 (위 결정 4의 Risks) — tune·holdout 모두 이번 실행에서 정답이긴 했지만
   표본이 각 1건뿐이라 별도 지표로 추적할 가치가 있다.
3. 포트폴리오·이력서에 이번 실측치 반영 (`NOTES.private.md`의 "수치 규율" 항목 갱신 포함).
